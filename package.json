{
  "name": "repobird-threejs-demos",
  "version": "1.0.0",
  "description": "Demos of visual apps built with RepoBird using Three.js.",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:browser": "vitest --browser --run",
    "test:browser:chrome": "vitest --browser=chrome --run",
    "test:browser:firefox": "vitest --browser=firefox --run",
    "test:browser:webkit": "vitest --browser=webkit --run"
  },
  "keywords": [
    "threejs",
    "vitest",
    "demo"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/browser": "^1.5.0",
    "jsdom": "^24.0.0",
    "playwright": "^1.44.0",
    "three": "^0.163.0",
    "vitest": "^1.5.0"
  }
}
